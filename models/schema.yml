version: 2

models:
  - name: census_base
    description: Adult income census data
    config:
      materialized: table
  - name: features
    config:
      materialized: table
    meta:
      fal:
        scripts:
          after:
            - train_deploy_sagemaker.py
  - name: earns_more_than_50k
    config:
      materialized: table
